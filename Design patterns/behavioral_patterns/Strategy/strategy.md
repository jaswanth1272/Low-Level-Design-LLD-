Strategy

Idea:
- Select a behavior/algorithm at runtime.

When:
- When you want to change behavior dynamically based on user choice.

Simple example:
- Pay via Credit Card or UPI based on what user selects.

How:
- Define different strategies (interfaces/classes) and choose one at runtime.