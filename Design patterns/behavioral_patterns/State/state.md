State

Idea:
- Change an object’s behavior based on its internal state.

When:
- When an object should behave differently depending on its state.

Simple example:
- A Traffic Light behaves differently if it’s in Red, Yellow, Green states.

How:
- Create different State classes and switch between them inside Context object.

