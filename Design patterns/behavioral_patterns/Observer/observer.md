Observer

Idea:
- Notify multiple objects when some event happens.

When:
- When one change needs to notify many.

Simple example:
- YouTube Notifications: When a new video is uploaded, all subscribers get notified.

How:
- List of Observers; publisher notifies them when event occurs.

