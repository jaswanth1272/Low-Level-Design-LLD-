Facade

Idea:
- Provide a simple interface to a complex system of classes.

When:
- To simplify usage when many subsystems are involved.
Simple example: A car’s start button handles starting engine, ignition, lights internally.
You just press one button.

How:
- Make a new Facade class that internally manages multiple complex classes.

