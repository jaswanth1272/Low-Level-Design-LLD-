Decorator

Idea:
- Add extra features to an object without modifying its original class.

When:
- When you want to add functionalities dynamically.
Simple example: Order a Pizza, then add extra cheese or extra toppings.

How:
- Wrap original object with a decorator object that adds functionality.