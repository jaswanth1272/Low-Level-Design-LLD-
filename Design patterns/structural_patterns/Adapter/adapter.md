Adapter

Idea:
- Connect two incompatible interfaces so that they can work together.

When:
- When you have old code but new code needs different method names.
Simple example: A power adapter connects a US plug to an Indian socket.

How: Write a small Adapter class that converts new method calls into old method calls.